# GitHub Build-from-Source Buildpack Configuration
# Copy this file to your application root as .buildpack-config

# GitHub repository owner (required)
GITHUB_REPO_OWNER="example-owner"

# GitHub repository name (required)
GITHUB_REPO_NAME="example-repo"

# Branch to clone (optional, defaults to "main")
BRANCH="main"

# Build tool to use (optional, defaults to "auto")
# Options: "auto", "maven", "gradle"
# "auto" will detect based on presence of pom.xml or build.gradle
BUILD_TOOL="auto"

# JAR name pattern to find after build (optional)
# If not specified, will find the first non-sources, non-javadoc JAR
# Examples: "*-all.jar", "myapp-*.jar", "application.jar"
JAR_NAME_PATTERN=""

# Auto-start JAR as background process (optional, defaults to "false")
# Set to "true" to automatically run the JAR in the background at startup
# Set to "false" to only add JAR to classpath without running it
AUTO_START="false"

# JVM options for auto-started JAR (optional)
# Only used if AUTO_START is "true"
# Example: "-Xmx512m -Xms256m -Dserver.port=8081"
JVM_OPTIONS=""
